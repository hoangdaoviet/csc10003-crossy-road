#pragma once
#include "Button.h"
#include "windows.h"

class handlerInput{
public:
    // virtual void handleInput(Button input) = 0;
    // virtual void changeState() = 0;
    // virtual ~handlerInput() {}
};
